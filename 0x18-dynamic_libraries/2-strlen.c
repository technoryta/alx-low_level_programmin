#include "main.h"
#include <string.h>

/**
* _strlen - Gets the lenght of a string
* @s: characters
* Return: Integer
*/

int _strlen(char *s)
{
	int lenght;

	lenght = strlen(s);
	return (lenght);
}
